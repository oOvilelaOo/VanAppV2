<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teste">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CadastroTransportadorActivity" android:parentActivityName=".LoginTransportadorActivity" />
        <activity android:name=".LoginTransportadorActivity" android:parentActivityName=".AberturaActivity" />
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".LoginActivity" android:parentActivityName=".AberturaActivity"  />
        <activity android:name=".CadastroActivity" android:parentActivityName=".LoginActivity" />
        <activity
            android:name=".AberturaActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetalhamentoTransportador_Activity"/>
    </application>

</manifest>